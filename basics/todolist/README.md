## TODO-List Generator

- Features
1. Create a task with title and description.
2. Get all the task from a file (tasks.json).

APIs Exposed:
1. put(taskList []Task) -> error
2. get() -> []Task, error
